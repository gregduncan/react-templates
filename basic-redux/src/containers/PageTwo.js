import React from "react"

export default class PageTwo extends React.Component {
    render() {
        return (
            <div>
                {this.props.children}
            </div>
        )
    }
}